import { FormateadorFechaPipe } from './formateador-fecha.pipe';

describe('FormateadorFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateadorFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
